{
  "name": "web-heic-converter",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-deploy": "npm run build && firebase deploy",
    "lint": "prettier --write . && oxlint"
  },
  "dependencies": {
    "heic-convert": "^2.1.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.20.1",
    "oxlint": "^0.9.9",
    "prettier": "^3.3.3",
    "vite": "5.4.8"
  }
}
